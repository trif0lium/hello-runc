---
- name: firecracker-containerd
  hosts: all
  tasks:
    - name: Install required dependencies
      ansible.builtin.shell: sudo /bin/sh /vagrant/ansible-playbooks/scripts/install-required-dependencies.sh
    - name: Install firecracker-containerd
      ansible.builtin.shell: sudo /bin/sh /vagrant/ansible-playbooks/scripts/install-firecracker-containerd.sh
